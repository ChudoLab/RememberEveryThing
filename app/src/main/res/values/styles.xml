<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your theme here. -->
        <item name="android:textSize">@dimen/bodyFont</item>
        <item name="android:lineSpacingExtra">@dimen/lineSpaceBody</item>
        <item name="android:colorActivatedHighlight">@color/colorPrimaryLight</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>


    </style>

    <style name="editText">
        <item name="android:padding">@dimen/paddingFont</item>
        <item name="android:textSize">@dimen/bodyFont</item>
        <item name="android:lineSpacingExtra">@dimen/lineSpaceBody</item>
        <item name="android:textColor">@color/textColorMain</item>
    </style>

    <style name="toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/headline</item>
    </style>

    <style name="mySwitch">
        <item name="android:padding">@dimen/paddingFont</item>

    </style>

    <style name="divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/colorAdditionalLight</item>
    </style>

    <style name="cardHolderLayout" parent="Theme.AppCompat.Light">
        <item name="android:paddingBottom">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:background">@color/colorCardHolderBack</item>
    </style>

    <style name="card" parent="Theme.AppCompat.Light">
        <item name="android:padding">8dp</item>
        <item name="android:layout_margin">12dp</item>
        <item name="android:background">@color/colorBackground</item>
    </style>


    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
